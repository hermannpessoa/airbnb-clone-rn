import { StyleSheet } from "react-native"
const styles = StyleSheet.create({
    bubbleMarker:{
        backgroundColor: '#fff',
        paddingHorizontal: 8,
        paddingVertical: 2,
        borderRadius: 50,
        borderWidth: 1,
        borderColor: '#ccc'
    },
    bubbleMarkerText:{
        fontWeight: 'bold',
        fontSize: 16
    }
})

export default styles;